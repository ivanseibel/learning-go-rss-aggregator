version: '3'
services:
  postgres:
    image: postgres
    container_name: postgres-dev
    environment:
      POSTGRES_PASSWORD: 
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
